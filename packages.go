// packages are prewritten code that you can use
// a way of putting similar code together
// fmt package has prewritten code that we can use
package main

import (
	"fmt"
)

func packageExplanation() {
	// from the fmt pacakge and calling println
	// if you see ... in the index, it means it can take in an unlimated amount of parameters
	// the paramter "...interfce{}" "gives as many arguments of any type as you'd like"
	//println is know as an identify in go
	fmt.Println("hiiii")

}
